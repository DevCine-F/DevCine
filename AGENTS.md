- **UI Rule:** Tuyệt đối KHÔNG sử dụng các Emoji/Icon trên text (VD: 🔒, ⚠️) trong các thông báo hoặc nhãn giao diện. Thay vào đó, hãy sử dụng các Material Symbols (nếu cần) hoặc chỉ dùng text thuần túy để giữ thiết kế sạch sẽ.
- **Showtime Management Rule:** Suất chiếu trong quá khứ/đang diễn ra (`startTime < now`) hoặc đã phát sinh vé đặt (`reserved > 0`) phải bị khóa toàn bộ thao tác Sửa & Xóa ở cả Backend (`ShowtimeService.java`) và Frontend (`ShowtimeDetailsDrawer.vue`, `ShowtimeDrawer.vue`). Không cho phép đổi Phòng, Phim, Định dạng và Giờ chiếu khi đã có vé. Chỉ cho phép cập nhật toàn diện khi chưa có vé bán và suất còn ở tương lai.
- **POS Ticket & Sweetbox Capacity Rule:** Ghế đôi Sweetbox có sức chứa = 2 vé (2 người xem), ghế đơn = 1 vé. Tại Bước 3 POS (`TicketingPOS.vue`), tổng số vé yêu cầu luôn bằng tổng sức chứa các ghế đã chọn; tự động gán `NGƯỜI LỚN = totalRequiredTickets`; vô hiệu hóa nút `[-]` khi số lượng vé đang bằng đúng `totalRequiredTickets`; cơ chế 1-click transfer tự động cân bằng vé; tính đúng tổng giá cho 2 vé Sweetbox và sinh đủ 2 phần tử `seatSelections` gửi Backend.
- **Seat & Order Hold Lifecycle & Countdown Timer Rule:** Hệ thống tách biệt 2 cấu hình thời hạn: (1) `SEAT_HOLD_MINUTES` (3–30 phút, mặc định 10) áp dụng cho giữ ghế trong phiên đặt vé trực tiếp (Online `BookingView.vue` và timer chọn ghế trên màn hình POS `TicketingPOS.vue`); (2) `POS_ORDER_HOLD_MINUTES` (3–60 phút, mặc định 15) áp dụng khi thu ngân bấm "Giữ đơn" để lưu đơn chờ tại quầy POS (`PendingOrderService.java` và Pinia store `usePosStore.js`). Cả hai đều được quản lý tập trung qua `SystemSettingService.java` và cấu hình tại `AdminSettings.vue`. Chu trình giữ ghế và đồng hồ đếm ngược tuân thủ nghiêm ngặt chuẩn rạp chiếu phim (Lotte/CGV): (a) **Kích hoạt giữ chỗ & Countdown**: Bước 1 chọn ghế Online (`BookingView.vue`) tuyệt đối KHÔNG chạy đồng hồ đếm ngược khi CSDL chưa tạo đơn; Countdown Timer chỉ kích hoạt sau khi khách hoàn tất Bước 1 và bấm "Tiếp tục" (`currentStep > 1`), gọi API `holdSeats` tạo đơn DB thành công (Early DB Hold), đồng thời tái dùng `heldBookingId` trong Pinia store `stores/booking.js` để cập nhật đơn liền mạch (re-hold) khi khách thêm/bớt F&B hoặc chọn Voucher; (b) **Chống lệch múi giờ (Timezone Skew)**: Backend `BookingController.java` trả về `ttlSeconds` ($\ge 0$, bọc `Math.max(0, ...)` chống âm/NPE) và `serverTime` (ISO 8601 offset `+07:00`); Frontend tính `holdDeadlineTs = Date.now() + ttlSeconds * 1000` làm nguồn sự thật tuyệt đối, không phụ thuộc đồng hồ máy khách hay `new Date(expiresAt)`; (c) **Cảnh báo $\le 120$s**: Banner đồng hồ đổi sang nền đỏ nhấp nháy (`bg-red-500/10 text-red-400 shadow-[0_0_15px_rgba(239,68,68,0.2)]`) và phát Toast cảnh báo 1 lần duy nhất; cờ `hasWarnedNearExpiry` tự động reset về `false` mỗi khi `holdDeadlineTs` được cấp mới; (d) **Page Visibility & Memory Cleanup**: Bắt sự kiện `visibilitychange` để bù lệch `nowTs = Date.now()` tức thì khi khách quay lại tab; `onUnmounted` dọn sạch `clearInterval(countdownTimer)` và gỡ bỏ listener chống zombie timer; (e) **Dọn ghế CSDL & Tranh chấp thân thiện**: `SeatHoldCleanupScheduler.java` và `BookingSeatRepository.java` quét giải phóng ghế theo `expiresAt <= NOW()` (kèm fallback `createdAt < cutoff` cho đơn cũ); `BookingService.java` ném ngoại lệ tranh chấp ghế tiếng Việt thân thiện mang tên ghế từ `s.displayLabel()` (VD: `"Ghế J1 đã có người đặt hoặc đang được giữ chỗ."`), bọc null-safe chống `NullPointerException`; (f) **Đồng bộ Nhả ghế WebSocket DB-Aware & Khóa vững khi Thanh toán / Chọn Voucher (DB-Aware WebSocket Seat Release & Atomic Re-Hold)**: Phân định rạch ròi 2 cơ chế giữ ghế (RAM WebSocket Lock tầng 1 và DB Hold tầng 2): (1) **Chống nhả ghế khi sang Cổng Thanh toán (VNPAY / VietQR)**: Tại `SeatLockService.releaseSession(sessionId)` (khi client disconnect do redirect VNPAY `window.location.href`), backend dùng `bookingSeatRepository.findConflictingSeats` lọc bỏ các ghế đã có đơn DB `HOLD` còn hạn (`expiresAt > now`), chỉ broadcast `SEAT_RELEASED` cho các ghế thuần click tạm ở Bước 1; (2) **Chống nhả ghế khi Re-hold (Chọn Voucher / F&B)**: `PosHoldService.releaseHold(bookingId, broadcastRelease)` hỗ trợ cờ tắt phát broadcast; `BookingService.holdSeats` gọi `releaseHold(..., false)` khi re-hold đơn cũ để cập nhật F&B/Voucher, triệt tiêu hoàn toàn sự kiện `SEAT_RELEASED` giả gửi tới POS làm nhả màu ghế; (3) **Tự động nhả ghế khi hết hạn**: `SeatHoldCleanupScheduler.java` khi quét dọn các đơn `HOLD` quá hạn tự động gom nhóm theo `showtimeId` và phát broadcast `SEAT_RELEASED` qua WebSocket để POS tự động mở lại ghế trống (`AVAILABLE`) đúng giờ mà không cần F5; (4) **Chủ động giải phóng khi Đổi suất / Quay lại / Rời trang**: Tại Bước 1 (`BookingView.vue`), khi khách bấm nút "Đổi suất khác" hoặc "Quay lại" hoặc chuyển trang (hook `onBeforeRouteLeave`), nếu đơn đã từng tạo DB hold (`store.bookingId`), Frontend tự động gọi `store.releaseHold()` và `store.resetSelections()` để hủy đơn DB và phát `SEAT_RELEASED` tức thì trong 0.1s.
- **Bank Account & VietQR Settings Rule:** Tại `AdminSettings.vue`, khối Tài khoản nhận tiền (QR chuyển khoản) phục vụ sinh mã VietQR cho cả Online (`BookingView.vue`) và POS (`TicketingPOS.vue`). Áp dụng nguyên tắc All-or-Nothing (hợp lệ khi đủ cả 3 trường Ngân hàng/STK/Chủ TK hoặc để trống cả 3). STK chỉ nhận số `0–9`, độ dài 4–20 số; Tên chủ tài khoản tự động chuẩn hóa IN HOA KHÔNG DẤU (A-Z và khoảng trắng, tối thiểu 2 ký tự). Kiểm tra và hiển thị lỗi trực tiếp (Inline Error đỏ `text-red-500`/`border-red-500`) theo thời gian thực khi người dùng thao tác, không dùng toast popup cho validate form.
- **Invoice Snapshot & Ticket Details Synchronization Rule:** Toàn bộ thông tin giá vé (`BookingSeat.priceSnapshot`), đơn giá F&B (`BookingFnb.priceSnapshot`), tên món (`itemNameSnapshot`), phụ thu (`surchargeSnapshot`), và tổng thanh toán (`totalPrice`, `finalPrice`) được đóng băng tuyệt đối theo Snapshot lúc mua; cấm đọc `fnbItem.getPrice()` live trong `AdminBookingController.java` làm sai lệch tiền hoá đơn khi Admin sửa giá. Màn hình Lịch sử đặt vé (`BookingHistoryView.vue`) và Quản lý Hoá đơn (`AdminBookings.vue`) phải đồng bộ 100%: hiển thị riêng `seatTotal` cho dòng "Tiền vé & Ghế" (không lấy nhầm `originalPrice`), chuẩn hóa nhãn loại ghế (`VIP - Người lớn x1`), tự động bóc tách chuỗi loại vé nhiều phần tử của ghế Sweetbox (`ticketType = "ADULT,ADULT"`) để hiển thị đúng đối tượng và số lượng vé (`Sweetbox - Người lớn x2`), hiển thị tiêu đề tổng vé thực tế `detailTotalTickets` (ví dụ `Vé xem phim (3)`), tự động flatMap khi In lại vé K80 (`buildInv`), và hiển thị tùy chọn vị/nước dạng danh sách bullet point sạch sẽ (`• Option (+phụ thu)`), không hiển thị tiền tố `Ô chọn...`.
- **Customer Booking History Filter Rule:** Màn hình Lịch sử đặt vé khách hàng (`/profile/history` qua `BookingController.java`) CHỈ hiển thị các đơn có trạng thái thành công (`CONFIRMED`, `COMPLETED`), tuyệt đối không trả về các đơn rác `HOLD`, `CANCELLED`, `EXPIRED`. Toàn bộ các trạng thái đơn vẫn được bảo toàn 100% trong Database và trên giao diện Quản trị Admin (`/admin/bookings`) để phục vụ đối soát tài chính và theo dõi vận hành.
- **Admin Booking Details & Status Deduplication Rule:** Modal Chi tiết hoá đơn (`AdminBookings.vue`) áp dụng Bố cục 2 Cột Cân Bằng (Cột Trái: Suất chiếu $\rightarrow$ Vé $\rightarrow$ Bắp nước $\rightarrow$ QR Check-in ngang; Cột Phải: Khách hàng & Thu ngân $\rightarrow$ Tổng kết thanh toán). Tuân thủ nguyên tắc "1 Nhiệm vụ - 1 Nhãn duy nhất": Thanh Header nắm giữ duy nhất nhãn trạng thái tổng thể đơn (`HOÀN TẤT`, `HẾT HẠN`, `ĐÃ HUỶ`, `ĐANG GIỮ`); Tiêu đề QR chỉ hiển thị badge soát vé (`ĐÃ CHECK-IN`, `QUÁ HẠN SUẤT CHIẾU`, `CHƯA CHECK-IN`) khi đơn là vé hợp lệ, đơn hết hạn/huỷ chỉ làm mờ QR kèm chú thích nhẹ; Khối Thanh toán chỉ hiển thị nhãn dòng tiền thuần túy (`ĐÃ THANH TOÁN`, `CHỜ THANH TOÁN`, `CHƯA THANH TOÁN`); Chuẩn hóa text `"Tiền vé"` và `"Phương thức:"`, nghiêm cấm viết tắt `"PT:"`.
- **UI Border Radius & Toolbar Standardization Rule:** Toàn bộ thanh công cụ lọc (Filter Toolbar), ô nhập liệu, dropdown select, nút bấm chức năng và chip bộ lọc trong phân hệ Quản trị (Admin) phải tuân thủ chuẩn bo góc sắc nét **`rounded-sm` (2px / `0.125rem`)** lấy theo mẫu chuẩn của `MovieToolbar.vue` ("BỘ LỌC NÂNG CAO"). Tuyệt đối KHÔNG lạm dụng `rounded-xl` (12px) hoặc `rounded-2xl` (16px) cho các nút và ô nhập liệu thanh công cụ vì gây hiệu ứng bo tròn quá mức dạng viên thuốc/capsule, làm vỡ tính thẩm mỹ sang trọng, góc cạnh của giao diện rạp phim.
- **POS Ticket Print & Gate Check-in Differentiation Rule:** Tại quầy POS (`TicketingController.java`), khi thanh toán thành công phải tự động lưu ngay `booking.setPrintedAt(now)` và `booking.setPrintedBy(soldBy)` vào CSDL (ghi nhận vé giấy K80 đã in lúc mua). Màn hình Kiểm soát vé (`TicketCheckIn.vue` / `TicketService.java`) phân tách tuyệt đối 2 nghiệp vụ: (1) Đơn Online chưa in vé (`alreadyPrinted == false`) $\rightarrow$ Đếm ngược 3s $\rightarrow$ Tự động mở cửa sổ in vé giấy K80 và cập nhật `printedAt = now`, `isCheckedIn = true`; (2) Đơn đã có vé giấy (Đơn POS hoặc Online đã lấy giấy, `alreadyPrinted == true`) $\rightarrow$ Tự động gọi endpoint `/api/tickets/checkin` để đánh dấu `isCheckedIn = true` $\rightarrow$ Hiển thị màn hình xanh *"Check-in Thành Công!"* cho khách vào phòng chiếu $\rightarrow$ **Tuyệt đối KHÔNG đếm ngược, KHÔNG tự động bật cửa sổ in lại vé lần 2**; (3) Chống gian lận: Vé đã check-in trước đó (`allCheckedIn == true`) hoặc quá giờ chiếu phải bị Backend chặn lại kèm thông báo rõ mốc thời gian (*"Mã đặt vé này đã được check-in vào lúc: HH:mm dd/MM/yyyy"*).
- **Banner & Movie Status Synchronization & Order Normalization Rule:** Hệ thống đồng bộ 2 chiều tuyệt đối giữa cờ `Movie.showOnBanner` và trạng thái `Banner.isActive` (cho banner `mode = MOVIE`) qua `BannerSyncService.java`: (1) Chiều Banner $\rightarrow$ Phim (`syncMovieFlag`): Khi bật/tắt hoặc xóa banner mode MOVIE, cập nhật `movie.showOnBanner` theo việc có tồn tại banner theo phim đang bật hay không (`existsByModeAndMovieIdAndIsActiveTrue`); (2) Chiều Phim $\rightarrow$ Banner (`applyMovieFlag`): Bật cờ `showOnBanner` bên Phim $\rightarrow$ tạo mới với thứ tự `maxOrder + 1` hoặc kích hoạt lại `isActive = true`; Tắt cờ bên Phim $\rightarrow$ chuyển `isActive = false` cho banner theo phim tương ứng; (3) Giao diện thẻ Banner (`AdminBanners.vue`): Tuân thủ nguyên tắc "1 Nhiệm vụ - 1 Nhãn duy nhất" (Góc trên bên trái nắm giữ duy nhất badge Mode `THEO PHIM` / `ẢNH`; Góc trên bên phải nắm giữ duy nhất badge Trạng thái hiệu lực toàn diện `ĐANG HIỂN THỊ`, `ĐANG TẮT`, `PHIM NGỪNG CHIẾU`, `ĐÃ HẾT HẠN`, `CHƯA TỚI HẠN`; Các nút thao tác ở đáy thẻ hiển thị icon trực tiếp không dùng vòng tròn nền xám mờ, bo góc `rounded-sm` chuẩn Admin UI); (4) **Hiển thị & Chuẩn hoá thứ tự thông minh (Intelligent Sequence & 1..N Normalization)**: Huy hiệu thứ tự trên tay cầm kéo hiển thị **`#{{ i + 1 }}`** theo vị trí visual ranking thực tế 1-based, đảm bảo chuỗi liên tục `#1, #2, #3...` phản hồi tức thì khi kéo thả, không hiển thị `#0`, không trùng lặp hay nhảy cóc; Frontend `fetchBanners()` và `deleteBanner` tự động phát hiện và đồng bộ ngầm (`persistOrder(true)`) về DB; Backend `BannerSyncService.normalizeOrders()` tự động chuẩn hoá và gom toàn bộ `displayOrder` trong CSDL thành chuỗi số nguyên liên tục `1..N` sau mọi thao tác (Thêm, Sửa, Xoá, Kéo-thả, Sinh banner từ phim và khởi động server qua `BannerBackfillRunner`).
- **Promotion & Private Voucher Rule:** Hệ thống hỗ trợ các nghiệp vụ khuyến mãi & voucher nâng cao: (1) **Voucher Riêng tư (Private / Hidden Voucher - `isHidden = true`)**: Tự động lọc ẩn khỏi trang Khuyến mãi công khai (`/promotions` và `/khuyen-mai`), chỉ người dùng có mã mới có thể nhập tại trang Ví voucher (`/profile/vouchers`) để lưu vào ví hoặc nhập trực tiếp khi đặt vé Online/POS; khi đã lưu vào ví thì hoạt động và hiển thị như voucher thông thường; trên giao diện Quản trị (`AdminPromotions.vue`), nhãn hiển thị là `RIÊNG TƯ` (tag tím `rounded-sm` text-`[7.5px]` chuẩn Admin UI), tiêu đề cấu hình không dùng icon; (2) **Áp dụng nhiều phim (Multi-movie Selection)**: Hỗ trợ chọn áp dụng cho danh sách nhiều phim cụ thể (`Promotion.applicableMovieIds` chuỗi CSV); khi cấp phát voucher (`Voucher.java`), toàn bộ danh sách ID và tên các phim áp dụng được đóng băng bất biến qua snapshot (`applicableMovieIdsSnapshot`, `applicableMovieTitlesSnapshot`) để bảo toàn quyền lợi của khách khi Admin sửa đổi chương trình khuyến mãi sau này; (3) **Thứ tự hiển thị trang Khuyến mãi (`/khuyen-mai`)**: Danh sách mã ưu đãi công khai (`/api/marketing/promotions/active` phía `MarketingController.java`) luôn được sắp xếp theo thứ tự mới nhất lên trên cùng (`ID DESC` / ID giảm dần), phân trang 6 mục/trang; (4) **Tự động làm mới số liệu Voucher (`AdminPromotions.vue`)**: Khi mở modal Chi tiết Voucher (`openDetail`) hoặc sau khi phát voucher cho khách (`handleIssueVoucher`), hệ thống phải tự động gọi `await fetchMarketingData()` để kéo dữ liệu mới nhất từ CSDL và đồng bộ lại `detailTarget`, đảm bảo các số liệu (lượt dùng `usedCount`, % tiến độ, trạng thái, email chiến dịch) luôn được cập nhật chính xác mà không cần F5; (5) **Trình bày Phim áp dụng thông minh (`AdminPromotions.vue`)**: Tại modal Chi tiết Voucher, dòng "Áp dụng theo phim" tự động phân nhánh: 0 phim hiển thị `Tất cả phim` (xám nhẹ), 1 phim hiển thị tên phim đầy đủ, $\ge$ 2 phim hiển thị Badge đếm tương tác màu vàng `[ X phim áp dụng > ]` bo góc `rounded-sm`; click vào Badge mở Modal danh sách Dark Mode cao cấp (`promoMoviesModalTarget`) hiển thị STT `#1..#N`, thumbnail poster, tên phim, thời lượng, thể loại và ô tìm kiếm phim theo tên, loại bỏ hoàn toàn khung tooltip trắng mặc định của trình duyệt; (6) **Ghim Voucher đang áp dụng lên đầu danh sách (`TicketingPOS.vue` & `BookingView.vue`)**: Tại màn hình POS và Đặt vé Online, danh sách Voucher khả dụng (`eligibleVouchers`) luôn tự động ghim voucher đang được chọn / áp dụng thành công lên vị trí ĐẦU TIÊN (index 0) kèm trạng thái active, các voucher còn lại được sắp xếp theo số tiền tiết kiệm giảm dần, giúp thu ngân và khách hàng thấy ngay mã đang dùng mà không cần cuộn tìm; (7) **Tối ưu hiển thị Voucher tại POS & Đặt vé (Voucher Selection Modal & Smart Movie Label)**: Tại quầy POS (`TicketingPOS.vue`), khu vực voucher bên ngoài được rút gọn thành 1 thanh bấm tinh gọn (hiển thị tóm tắt mã đang dùng kèm số tiền giảm, nút [Đổi mã] và [Bỏ chọn]), bấm vào mở **Modal Chọn Voucher Khuyến Mãi** (`isVoucherModalOpen`) với chiều cao cuộn độc lập, tích hợp ô nhập mã trực tiếp, danh sách voucher khả dụng và Accordion thu gọn [Ưu đãi chưa đủ điều kiện]; đồng thời hàm `formatPromoMovieLabel` tự động rút gọn danh sách nhiều phim thành `Áp dụng: [Tên phim] (+N phim khác)` trên cả `TicketingPOS.vue` và `BookingView.vue`, loại bỏ hoàn toàn tình trạng in tràn danh sách hàng chục bộ phim làm vỡ layout thẻ voucher; (8) **Sắp xếp Lịch sử Voucher (`VouchersView.vue` & `VoucherRepository.java`)**: Tab "Lịch sử voucher" luôn ưu tiên hiển thị voucher vừa sử dụng mới nhất lên trên cùng (`usedAt DESC`), nếu là voucher hết hạn chưa dùng thì sắp theo hạn sử dụng (`validUntil DESC`), đảm bảo mọi giao dịch thanh toán vé vừa xong sẽ đưa mã voucher lên ngay vị trí đầu tiên của Trang 1; (9) **Phân biệt Thông báo Lỗi Voucher Cá nhân & Hệ thống (`VoucherService.java` & `VoucherController.java`)**: Khi khách nhập mã giảm giá (POS hoặc Online), hệ thống ưu tiên kiểm tra `existsUsedByCustomerIdAndPromotionId` để trả về chính xác `"Bạn đã sử dụng mã ưu đãi này rồi."` khi chính tài khoản đó đã dùng trước đó, phân biệt rạch ròi với lỗi cạn ngân sách toàn hệ thống `"Mã ưu đãi đã hết lượt sử dụng trên toàn hệ thống."`; (10) **Ràng buộc Số vé tối đa được giảm theo Cấu hình rạp (Dynamic Max Ticket Quantity Validation)**: Ô "Số vé tối đa được giảm / đơn" (`maxTicketQuantity`) trên Form Tạo/Sửa Voucher (`AdminPromotions.vue`) và Backend (`MarketingController.java`) được ràng buộc linh hoạt theo cấu hình hệ thống `MAX_TICKETS_PER_BOOKING` (mặc định 8 vé): giá trị `0` biểu thị giảm cho toàn bộ vé trong đơn; giá trị `> 0` bắt buộc nằm trong khoảng $[1 \dots MAX\_TICKETS\_PER\_BOOKING]$, hiển thị lỗi trực tiếp theo thời gian thực (Inline Error) và chặn lưu khi vượt trần; (11) **Hợp nhất Modal Voucher & Điều hướng View/Edit (Unified Voucher Modal & Smooth Mode Switching)**: Hợp nhất Modal Chi tiết và Form Tạo/Sửa Voucher vào 1 khung Modal 2 Cột Sleek Minimalist duy nhất (`isVoucherDrawerOpen`), chuẩn hóa 3 tiêu đề đồng bộ (`TẠO MÃ GIẢM GIÁ MỚI`, `CẬP NHẬT MÃ GIẢM GIÁ`, `CHI TIẾT MÃ GIẢM GIÁ`), hỗ trợ chuyển đổi 1-click giữa View $\leftrightarrow$ Edit; khi mở các popup thao tác phụ (Phát voucher cho khách `issueTarget`, Gửi email chiến dịch `emailTarget` với `z-[1050]`), Modal Chi tiết (`z-[1000]`) vẫn được giữ nguyên phía sau; khi đóng popup phụ hoặc thực hiện xong, hệ thống tự động quay về Modal Chi tiết và đồng bộ lại `detailTarget` với dữ liệu mới nhất; (12) **Chuẩn hóa Thuật ngữ & Header Quản lý Khuyến mãi (`AdminPromotions.vue`)**: Tiêu đề trang chuẩn hóa thành `QUẢN LÝ KHUYẾN MÃI`, mô tả phụ `Quản lý mã giảm giá, chương trình ưu đãi và tin tức khuyến mãi`, nút chức năng `Tạo mã giảm giá` / `Tạo tin khuyến mãi`, tab `Mã giảm giá` / `Tin khuyến mãi`, loại bỏ hoàn toàn các từ tiếng Anh không đồng bộ (`Marketing Hub`, `Vouchers`); (13) **Bảo vệ Dữ liệu 3 Tầng & Báo lỗi Inline Zero-Layout-Shift (3-Layer Protection & Zero Layout Shift Inline Validation)**: Form Tạo/Cập nhật voucher (`AdminPromotions.vue`) áp dụng mô hình bảo vệ 3 tầng: (Tầng 1 - Keystroke & Input Masking) tự động chuẩn hóa mã code IN HOA không dấu, giới hạn độ dài $[3 \dots 15]$ ký tự (`^[A-Z0-9]{3,15}$`) ở cả Frontend và Backend `MarketingController.java`, format tiền tệ phân tách hàng nghìn và chặn keystroke làm $\% > 100$; (Tầng 2 - Realtime & On Blur Validation) tự động kiểm tra vi phạm trần số học, ràng buộc đơn tối thiểu $\ge$ mức giảm tiền cố định, số vé tối đa $\le MAX\_TICKETS\_PER\_BOOKING$, ngày hết hạn $\le$ ngày bắt đầu; (Tầng 3 - Save Validation & Auto-Focus) quét toàn diện 15 trường khi bấm Lưu, bắt buộc chọn ít nhất 1 phim khi bật chế độ "Phim được chọn", tự động cuộn mượt và focus vào ô lỗi đầu tiên; toàn bộ ô nhập liệu sử dụng container thông báo cố định `min-h-[16px]` để khi xuất hiện thông báo lỗi đỏ (`text-[10.5px] text-red-400 font-medium`) sẽ thay thế mượt mà vị trí helper text xám mà không làm giật/xô lệch layout các ô bên cạnh; (14) **Đồng bộ Khối Cài đặt Nâng cao & Loại bỏ Cộng dồn mã (Unified Settings & Single Voucher Policy)**: Do chính sách an toàn tài chính của rạp quy định mỗi đơn hàng chỉ được dùng duy nhất 1 voucher (`isStackable = false`), modal Xem chi tiết Voucher (`AdminPromotions.vue`) loại bỏ hoàn toàn dòng hiển thị dư thừa `"Cộng dồn mã khác: Không"`, thay thế bằng dòng thông tin `"Chế độ hiển thị:"` (hiển thị tag tím `[ Riêng tư ]` hoặc text xanh `Công khai`), đồng bộ 100% với các trường cấu hình trên Form Tạo/Sửa; (15) **Phòng vệ Chống Sử dụng Trùng Voucher Đa kênh & Đa luồng (2-Tier Voucher Double-Spending Protection & Atomic Lock)**: Hệ thống thiết lập cơ chế bảo vệ 2 tầng nghiêm ngặt chống gian lận dùng 1 voucher cho nhiều đơn hàng đồng thời (Online và POS hoặc đa tab/thiết bị): (Lớp 1 - Chặn giữ chỗ `holdSeats`) Query `BookingRepository.isVoucherHeldByOtherBooking` quét các đơn `HOLD`, `PENDING_PAYMENT`, `PAYING` còn hiệu lực (`expiresAt > now`), từ chối tạo đơn thứ hai cùng giữ voucher kèm thông báo `"Mã ưu đãi đang được giữ trong một phiên giao dịch khác của bạn. Vui lòng hoàn tất hoặc hủy phiên đó trước."`, ngoại trừ trường hợp re-hold cập nhật chính đơn đó qua `heldBookingId`; (Lớp 2 - Atomic Lock khi thanh toán `completePayment`) Thực thi câu lệnh nguyên tử `VoucherRepository.markVoucherAsUsedIfUnused` (`UPDATE vouchers SET is_used = true, used_at = :now WHERE id = :id AND is_used = false`) đặt ngay đầu luồng thanh toán trước mọi thay đổi điểm/vé/trạng thái; (16) **Đồng bộ Khóa Giữ & Nhả Voucher Đa Kênh Thời Gian Thực (Two-Way Immediate Voucher Hold & Release with Redis Lease & WebSocket)**: Giải quyết triệt để xung đột khi thu ngân tại quầy POS và khách hàng Online cùng mở ví voucher hoặc nhập cùng một mã khuyến mãi: (a) **Cơ chế Khóa Độc quyền Tạm thời (Redis Lease Key `voucher:lease:{voucherId}`)**: Được quản lý bởi `VoucherHoldLeaseService.java` với TTL mặc định 600s (Online) / 900s (POS) lưu thông tin channel, sessionId, customerId; Định danh phiên Online sử dụng **Browser Tab Session Token cố định** (`sessionStorage`) thay vì `bookingId` để chống nhảy số session khi re-hold/tạo đơn DB mới; Tự động cho phép khách hàng Online **kế thừa/reclaim** lại khóa của chính mình khi F5 hoặc mở lại giỏ hàng (cùng `channel = ONLINE` và cùng `customerId`), tuyệt đối không tự khóa chính mình; (b) **Chặn Xung đột Chéo POS ↔ Online Ngay Tại Bước 3**: Nếu một kênh đang giữ mã (POS giữ hoặc Online giữ), bên còn lại cố tình chọn hoặc áp mã sẽ bị từ chối ngay lập tức kèm thông báo trực quan theo ngữ cảnh (`"Mã ưu đãi đang được áp dụng tại quầy thu ngân."` hoặc `"Mã ưu đãi đang được giữ trong một phiên đặt vé Online của bạn."`); (c) **Đồng bộ Tức thì qua WebSocket (Realtime Push Sync)**: Khi có sự kiện chiếm khóa (`VOUCHER_LEASE_ACQUIRED`) hoặc giải phóng khóa (`VOUCHER_LEASE_RELEASED`), backend tự động broadcast qua `/topic/customer/{customerId}/vouchers` và `/topic/voucher-updates`; Frontend tích hợp composable `useVoucherRealtime.js` tự động lắng nghe: nếu quầy POS áp mã của khách, màn hình Online của khách ngay lập tức vô hiệu hóa thẻ voucher đó mà không cần F5/tải lại trang (và ngược lại); nếu voucher đang chọn bị chiếm ở kênh kia thì tự động nhả và hiển thị cảnh báo; (d) **Chu trình Giải phóng Khóa Toàn diện (5-Tier Lease Cleanup)**: Khóa Redis được giải phóng tức thì khi: khách bấm bỏ chọn/đổi voucher, đổi thành viên/huỷ giỏ, rời trang/unmount (`onUnmounted`), thanh toán thành công (`completePayment`), hoặc tự động hết hạn an toàn theo TTL của Redis mà không gây treo mã vĩnh viễn.
- **Staff Management & Account Lifecycle Rule:** Hệ thống quản lý nhân sự (`StaffManager.vue`, `StaffController.java`, `AuthService.java`) áp dụng các chuẩn mực toàn diện: (1) **Layout Bảng Nhân sự**: Cấu trúc `table-fixed` với tỷ lệ độ rộng cố định (`Nhân viên 26%`, `Mã NV 11%`, `Cơ sở 20%`, `Vai trò 13%`, `Ngày gia nhập 12%`, `Trạng thái 11%`, `Thao tác 7%`), thu ngắn khoảng cách cột nhân viên và chống dãn lệch trên màn hình rộng; (2) **Ma trận Validate & Input Masking 3 Tầng**: (a) Họ và tên: Bắt buộc, $[2 \dots 30]$ ký tự (`maxlength="30"`), chặn số/ký tự đặc biệt (`^[\p{L}\p{M} ]+$`), tự động chuẩn hóa Title Case khi `blur`; (b) Tài khoản đăng nhập: Bắt buộc khi tạo, $[3 \dots 15]$ ký tự (`maxlength="15"`), Regex `^[a-z0-9_]{3,15}$`, tự động loại bỏ dấu tiếng Việt, khoảng trắng và chuyển chữ thường; khóa cứng (`disabled`) khi sửa; (c) Mã nhân viên: Khóa cứng (`disabled`), tự động lấy mã tiếp theo (`DCxxx`) qua `getNextCode()` (hỗ trợ cả unwrap chuỗi trực tiếp từ Axios Interceptor và envelope object); (d) Email: Bắt buộc, tối đa 100 ký tự, chuẩn RFC 5322, kiểm tra trùng `existsByEmailAndIdNot` khi sửa; (e) Số điện thoại: Bắt buộc đúng **10 chữ số** (`^\d{10}$`), loại bỏ hoàn toàn ràng buộc theo danh sách đầu số nhà mạng cố định, kiểm tra trùng `existsByPhoneAndIdNot` khi sửa; (f) Cơ sở & Vai trò: Bắt buộc chọn từ danh sách, không được để trống; (3) **Modal Bàn Giao Tài Khoản (Credentials Handover Modal)**: Sau khi tạo thành công, modal kết quả hiển thị thẻ thông tin chuẩn bo góc `rounded-sm` chứa đầy đủ: Họ tên, Mã NV kèm badge vai trò, Cơ sở làm việc, Tài khoản, Mật khẩu tạm (`DevCine@2026`), dòng trạng thái email đích (`Đã gửi email thông tin đăng nhập tới: [email]`), hỗ trợ nút sao chép nhanh 1-click riêng biệt cho từng trường và nút [Sao chép tất cả]; (4) **Phân Quyền & Chống Lạm Quyền Khi Sửa**: Khi tự sửa thông tin bản thân (Self-edit), hệ thống khóa hoàn toàn các trường Vai trò, Cơ sở và Trạng thái (chống tự nâng quyền hoặc tự khóa tài khoản); Quản lý cơ sở (`MANAGER`) chỉ được sửa nhân viên (`STAFF`) cùng cơ sở, không được sửa Quản lý khác/Admin và không được tự ý nâng quyền; (5) **Cơ Chế Popup Xác Nhận Thông Minh (Sensitive Change Confirmations)**: Khi bấm [Lưu thay đổi], hệ thống tự động phát hiện và bật modal xác nhận cho 3 trường hợp nhạy cảm: (a) Chuyển sang Tạm ngưng (`isActive: true -> false` cảnh báo ngắt quyền đăng nhập quầy POS); (b) Thay đổi Vai trò (`STAFF <-> MANAGER` nêu rõ nâng/hạ quyền); (c) Chuyển Cơ sở làm việc (cảnh báo điều chuyển quyền mở ca POS sang rạp mới); Sửa thông tin thông thường (Họ tên, SĐT, Email) được lưu trực tiếp; Đồng thời bảo vệ dữ liệu chưa lưu (Unsaved Changes) cảnh báo khi người dùng đóng modal lúc đang sửa dở; (6) **Phân Biệt Thông Báo Khóa Tài Khoản Nội Bộ & Khách Hàng (`AuthService.java`)**: Khi đăng nhập với tài khoản bị tạm ngưng (`isActive == false`), hệ thống tự động phân tách 2 thông điệp độc lập: (a) Tài khoản nội bộ (`STAFF`, `MANAGER`, `ADMIN`) trả về chuẩn nhân sự doanh nghiệp: `"Tài khoản nhân viên của bạn đã bị tạm ngưng hoạt động. Vui lòng liên hệ Quản lý cơ sở hoặc Quản trị viên hệ thống để được hỗ trợ."`; (b) Tài khoản khách hàng (`CUSTOMER`) trả về: `"Tài khoản của bạn đã bị tạm khóa do vi phạm chính sách hoặc theo yêu cầu. Vui lòng liên hệ bộ phận Chăm sóc khách hàng hoặc Hotline để được hỗ trợ."`.
- **POS Member Card & Voucher Presentation Rule:** (1) **Thẻ Thành Viên Mini 2 Cột (`TicketingPOS.vue`)**: Khối thông tin thành viên tại Bước 5 POS được chuẩn hóa thành thẻ 2 cột tinh gọn tiết kiệm 50% chiều cao (Cột Trái: Icon avatar đại diện + Tên khách in hoa đậm + SĐT chuẩn hóa định dạng cụm `0901 234 567`; Cột Phải: Badge Hạng thẻ có màu sắc nhận diện riêng theo rank `DIAMOND`/`GOLD`/`SILVER` + Dòng điểm tích lũy nổi bật `Điểm: 8.592`; Nút `[close Bỏ thẻ]` đặt ở góc trên bên phải dạng chip tinh gọn); (2) **Tinh giản Thẻ Voucher Khả dụng (`TicketingPOS.vue` & `BookingView.vue`)**: Thẻ voucher khả dụng loại bỏ hoàn toàn các thông số kỹ thuật dư thừa (`Tối đa X vé`, `Đơn từ Xđ`, `HSD` do hệ thống đã xác nhận thỏa mãn điều kiện), chỉ giữ lại Mã code, Tag % giảm, Tên chương trình, Nhãn phim thông minh và **Số tiền tiết kiệm to màu xanh lá (`Tiết kiệm Xđ`)**.
- **Admin Cinema State & URL Query Synchronization Rule:** Tại phân hệ Quản trị Cụm Rạp (`CinemaManager.vue`), toàn bộ trạng thái cơ sở đang chọn (`selectedCinema`) và tab chức năng (`activeTab` gồm `infrastructure`, `showtimes`, `config`,...) phải được đồng bộ 2 chiều qua URL Query Parameters (`?cinemaId=X&tab=Y` qua `useRoute` và `useRouter`). Khi người dùng bấm chọn rạp hoặc chuyển tab, URL tự động cập nhật ngầm bằng `router.replace`; khi bấm nút quay lại (`arrow_back`), query parameters được dọn sạch về `/admin/cinemas`; khi người dùng F5 (tải lại trang), hàm `syncStateFromRoute()` trong hook `onMounted` tự động đọc `route.query` để khôi phục chính xác cơ sở và tab đang đứng, đồng thời `watch(route.query)` đảm bảo điều hướng mượt mà với nút Back/Forward của trình duyệt mà không làm vỡ hoặc mất trạng thái.
- **Customer Showtime State & URL Query Synchronization Rule:** Tại màn hình Lịch chiếu khách hàng (`LichChieuView.vue`), toàn bộ trạng thái rạp đang chọn (`selectedCinemaId`) và ngày chiếu (`selectedDate`) phải được đồng bộ 2 chiều qua URL Query Parameters (`?cinemaId=X&date=YYYY-MM-DD` qua `useRoute` và `useRouter`). Khi khách hàng chọn rạp hoặc đổi ngày chiếu, URL tự động cập nhật ngầm bằng `router.replace`; khi bấm nút "Đổi rạp" (`changeCinema`), query parameters được dọn sạch về `/lich-chieu`; khi khách hàng F5 (tải lại trang), hàm `syncStateFromRoute()` trong hook `onMounted` tự động đọc `route.query` để khôi phục chính xác rạp và ngày chiếu đã chọn, đồng thời `watch(route.query)` đảm bảo điều hướng mượt mà với nút Back/Forward của trình duyệt mà không bị quay về màn hình chọn rạp.
- **Customer UI Theme Toggle & Notification Invalidation Rule:** Hai tính năng "Chuyển đổi giao diện Sáng/Tối" (`useTheme` / `toggleTheme`) và "Hệ thống Thông báo Khách hàng" (`useNotificationStore` / `isNotifModalOpen` / `/notifications`) được tạm thời VÔ HIỆU HÓA HOÀN TOÀN và ẩn khỏi giao diện khách hàng (`Navbar.vue`), coi như không có trong phạm vi giai đoạn phát triển và nghiệm thu hiện tại. Toàn bộ giao diện khách hàng vận hành theo Dark theme điện ảnh mặc định. Tuyệt đối không tự ý thêm lại icon Theme toggle hoặc icon Thông báo vào Header/Navbar của phân hệ khách hàng khi chưa có yêu cầu từ người dùng.
- **Customer Profile & Avatar Management Rule:** Hệ thống quản lý ảnh đại diện khách hàng đồng bộ từ CSDL (`User.avatarUrl`) đến giao diện (`ProfileInfoView.vue` và `AdminCustomers.vue`): (1) **Quyền Upload & Lưu trữ CSDL**: Cho phép mọi tài khoản đã đăng nhập (`@PreAuthorize("isAuthenticated()")` tại `UploadController.java`) tải ảnh lên Cloudinary; endpoint `PUT /api/customers/{id}` tại `CustomerController.java` tiếp nhận và lưu `avatarUrl` vào Entity `User` liên kết 1-1 với `Customer`; (2) **Hiển thị & Fallback Ảnh trống**: Khi có ảnh (`avatarUrl != ''`), render ảnh thực tế; khi không có ảnh (`avatarUrl` null/rỗng), hiển thị Avatar trống mặc định với icon Material Symbol `person` sang trọng trên nền tối; (3) **Tương tác Hover Đổi ảnh**: Khung avatar tại trang Thông tin cá nhân (`ProfileInfoView.vue`) không dùng nút/icon ghim cố định ở góc; thay vào đó, khi rê chuột (hover) vào khung avatar sẽ kích hoạt lớp phủ mờ tối `bg-black/60` kèm icon máy ảnh `photo_camera` và nhãn `ĐỔI ẢNH` ở chính giữa; (4) **Modal Chỉnh sửa Ảnh**: Cung cấp 2 lựa chọn tinh gọn: (a) "Tải ảnh từ thiết bị" (JPG, PNG, WEBP $\le$ 5MB, nén tự động trước khi upload); (b) "Đặt ảnh trống (Mặc định)" để reset về avatar trắng mặc định; đồng bộ ngay với `authStore.user` và `localStorage` sau khi lưu.
- **Seat Type Pricing & Surcharge Management Rule:** Hệ thống định giá vé áp dụng mô hình phụ thu theo loại ghế (Chuẩn Lotte Cinema - Phương án 1): (1) **Công thức Định giá Toàn diện**: $\text{Giá vé} = \text{Giá nền}(\text{Phòng} \times \text{Ngày} \times \text{Đối tượng}) + \text{Phụ thu Định dạng}(2\text{D}/3\text{D}) + \text{Phụ thu Loại ghế}(\text{Thường}/\text{VIP}/\text{Sweetbox})$; (2) **Cơ sở dữ liệu & Entity (`SeatType.java` & `DataSeeder.java`)**: Bảng `seat_types` lưu 2 mức phụ thu gồm `surcharge` (phụ thu ngày thường T2–T5) và `weekendSurcharge` (phụ thu cuối tuần T6–CN & ngày lễ, để trống tự động lấy bằng ngày thường); (3) **Quy tắc Ghế đôi Sweetbox**: Ghế đôi Sweetbox có sức chứa = 2 vé (`capacity = 2`), mức phụ thu cấu hình trong DB là phụ thu trên từng vé (khi tính tổng phụ thu thì tự động nhân theo sức chứa vé của ghế); (4) **Giao diện Quản trị Cấu hình Giá (`AdminPricing.vue`)**: Tab "Loại ghế" quản lý mức phụ thu cho từng loại ghế với tên gọi chuẩn hóa trực quan: `Ghế Thường`, `Ghế VIP`, `Ghế Đôi Sweetbox` (tuyệt đối không kèm phần mở ngoặc đơn `(Standard)`, `(Sweet Spot)` và không kèm mô tả phụ hoặc icon ghế bên cạnh tên ghế để giữ bảng hiển thị sạch sẽ, tối giản); Form tuân thủ chuẩn bo góc sắc nét `rounded-sm`, định dạng tiền tệ phân tách hàng nghìn, inline validation thời gian thực kiểm tra cuối tuần $\ge$ ngày thường; (5) **Đồng bộ Sơ đồ ghế & Đặt vé Online (`BookingView.vue`) / POS (`TicketingPOS.vue`)**: Thanh chú thích sơ đồ ghế (Seat Legend) giữ nguyên giao diện sạch sẽ, không hiển thị nhãn phụ thu; Khi khách hoặc thu ngân chọn ghế, hệ thống tự động bóc tách từ `priceTable[seat.seatType][audience]` để tính chính xác tổng tiền vé và lưu `priceSnapshot` bất biến vào CSDL.
- **Admin Permission System & 18-Tab Sidebar Alignment Rule:** Hệ thống phân quyền quản trị (`AdminPermissions.vue`, `RolePermissionController.java`, `PermissionService.java`, `DataSeeder.java`) được đồng bộ theo các Tab tính năng phân vào **6 Nhóm Phân Hệ Ngang**: (1) *Vận hành & Quầy vé* (`dashboard_stats`, `pos_ticketing`, `ticket_checkin`, `bookings`); (2) *Phim & Nội dung* (`movies`, `schedules`, `movie_categories`, `banners`); (3) *Rạp & Hạ tầng* (`cinemas`, `fnb_menu`); (4) *Kinh doanh & Khách hàng* (`pricing`, `promotions`, `customers`, và 2 mục Admin-only: `admin_reviews`, `admin_faqs`); (5) *Nhân sự* (`staff_management`); (6) *Hệ thống* (`roles`, `audit_logs`, `settings`). Phân tách độc lập quyền Quản lý phim (`movies:view,add,edit,delete`) và Lịch chiếu & Suất chiếu (`schedules:view,add,edit,delete`), quyền Kiểm soát vé (`ticket_checkin:view`) và Danh mục phim (`movie_categories:view,add,edit,delete`) thay vì gộp chung; Phân tách quyền Hoá đơn Quản trị (`GET /api/admin/bookings` gác `@perm.can('bookings', 'view')`) và Chi tiết hoá đơn đơn lẻ phục vụ in vé tại quầy POS (`GET /api/admin/bookings/{id}` gác `@perm.can('bookings', 'view') or @perm.can('pos_ticketing', 'view')`), đảm bảo nhân viên `STAFF` không truy cập được tab Hoá đơn trên sidebar nhưng vẫn in được vé K80 sau khi thanh toán QR chuyển khoản / tiền mặt; Giữ nguyên 100% phong cách thiết kế UI gốc cao cấp (Pill buttons chọn Role `rounded-full` với scale + shadow vàng rực rỡ khi active, thanh tab ngang với vạch neon vàng `glow underline indicator`, các khối thẻ Card bo góc `rounded-xl` nền `bg-surface-container-low`, huy hiệu đếm quyền viên thuốc vàng kim `ĐÃ CHỌN: X / Y`, master checkbox bật/tắt toàn bộ hành động của tab, và floating glassmorphic footer tóm tắt quyền kèm nút Lưu Thay Đổi); Chuẩn hóa toàn bộ hệ thống phân quyền theo mô hình **Pure RBAC (Theo Vai Trò: `ADMIN`, `MANAGER`, `STAFF`)**, loại bỏ hoàn toàn cấu hình cấp quyền riêng lẻ từng cá nhân (`Staff Overrides`) để đảm bảo tính an toàn, dễ kiểm toán và tránh rác quyền trong vận hành thực tế.
- **Admin Multi-Branch Data Scoping & Cinema Isolation Rule:** Hệ thống áp dụng triệt để nguyên tắc Phân tách Dữ liệu Cơ sở (Cinema Scoping / Multi-branch Data Isolation) theo mô hình chuỗi rạp chuyên nghiệp: (1) **Phân quyền Backend**: Phương thức `SecurityUtils.getCurrentUserCinemaId()` tự động trích xuất `cinemaId` từ tài khoản đăng nhập; Các vai trò Quản lý (`MANAGER`) và Thu ngân (`STAFF`) bị **khóa cứng phạm vi dữ liệu 100% theo cụm rạp được phân công** (Dashboard, Lịch chiếu, Bán vé POS, Hóa đơn, Nhân sự cơ sở); nếu tài khoản chưa được gán rạp thì Backend ném ngoại lệ `AccessDeniedException` ngăn chặn leo thang quyền; (2) **Quản trị viên tối cao (`ADMIN`)**: Nhận diện `cinemaId = null` (mặc định xem tổng thể toàn bộ chuỗi rạp) đồng thời hỗ trợ tham số `cinemaId` để xem lọc chi tiết từng rạp cụ thể; (3) **Giao diện Bảng điều khiển (`Dashboard.vue`)**: Dropdown lọc chọn cụm rạp (`[ Tất cả cụm rạp ]` / `[ Rạp A ]`) chỉ hiển thị độc quyền cho tài khoản `ADMIN` với chuẩn bo góc `rounded-sm`; đối với `MANAGER`, hệ thống ẩn hoàn toàn dropdown chọn rạp, ghim huy hiệu cơ sở trên Header và tự động render dữ liệu doanh số/suất chiếu độc quyền của rạp đó.


